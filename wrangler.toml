name = "love-page"
pages_build_output_dir = "public"
compatibility_date = "2025-01-01"

# Ya no se usa KV. El contenido y las fotos se guardan directo en este
# mismo repo de GitHub (carpeta content/ y public/gallery, public/place)
# vía la API de GitHub. Ver README.md para configurar el secreto
# GITHUB_TOKEN que hace falta para que esto funcione.
